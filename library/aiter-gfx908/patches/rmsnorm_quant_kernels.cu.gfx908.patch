--- a/csrc/kernels/rmsnorm_quant_kernels.cu
+++ b/csrc/kernels/rmsnorm_quant_kernels.cu
@@ -143,7 +143,13 @@
             vec2_f* thread_data_float2 = reinterpret_cast<vec2_f*>(&thread_data_float);
             for(int i = 0; i < thread_data_size / 2; i++)
             {
+#if defined(__gfx908__)
+                // gfx908 (CDNA1) lacks packed v_pk_mul_f32; scalar fallback.
+                thread_data_float2[i][0] = thread_data_float2[i][0] * rcp[0];
+                thread_data_float2[i][1] = thread_data_float2[i][1] * rcp[1];
+#else
                 asm volatile("v_pk_mul_f32 %0, %1, %2" : "=v"(thread_data_float2[i]) : "v"(thread_data_float2[i]), "v"(rcp));
+#endif
             }
             
             float* thread_data_weight2 = reinterpret_cast<float*>(&thread_data_weight);
@@ -152,6 +158,18 @@
                 vec2_f& thread_data_weight_float2 = rcp;
                 // thread_data_weight_float2[0] = ck_tile::type_convert<float>(thread_data_weight[2 * i]);
                 // thread_data_weight_float2[1] = ck_tile::type_convert<float>(thread_data_weight[2 * i + 1]);
+#if defined(__gfx908__)
+                // gfx908 (CDNA1): convert the packed weight pair via portable
+                // type_convert and multiply per-element (no packed/SDWA asm).
+                thread_data_weight_float2[0] =
+                    ck_tile::type_convert<float>(thread_data_weight[2 * i]);
+                thread_data_weight_float2[1] =
+                    ck_tile::type_convert<float>(thread_data_weight[2 * i + 1]);
+                thread_data_float2[i][0] =
+                    thread_data_float2[i][0] * thread_data_weight_float2[0];
+                thread_data_float2[i][1] =
+                    thread_data_float2[i][1] * thread_data_weight_float2[1];
+#else
                 if constexpr(std::is_same_v<DTYPE_I, ck_tile::bf16_t>)
                 {
                     asm volatile(
@@ -171,6 +189,7 @@
                     );
                 }
                 asm volatile("v_pk_mul_f32 %0, %1, %2" : "=v"(thread_data_float2[i]) : "v"(thread_data_float2[i]), "v"(thread_data_weight_float2));
+#endif
             }
 
             if constexpr(FUSE_QUANT)
